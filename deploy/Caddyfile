# Automatic HTTPS for citynovus.com. Point the A records at the droplet first.
citynovus.com {
  encode gzip
  reverse_proxy api:8080
}
www.citynovus.com {
  redir https://citynovus.com{uri} permanent
}
